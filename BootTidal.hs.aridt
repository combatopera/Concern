:set -XOverloadedStrings
:set prompt ""
:load Sound.Tidal.Context

(cps, nudger, getNow) <- cpsUtils'

$join$map(
    $(channels)
    $.[(d$(n),t$(n)) <- superDirtSetters getNow $(port) $(n)
])

let bps x = cps (x/2)
let hush = mapM_ ($'($) silence) [$join($map($(channels) d$(n)) ,)]
let solo = (>>) hush

:set prompt "tidal> "
