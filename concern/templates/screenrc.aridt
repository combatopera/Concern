vbell off
split -v
focus
resize $(consumer size)
screen $join($map($,(window consumer command) $"$()) $.( ))
title $(window consumer name)
scrollback $(consumer scrollback)
focus
screen $join($map($,(window vim command) $"$()) $.( ))
title $(window vim name)
