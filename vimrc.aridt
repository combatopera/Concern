source $(vimrc systemPath)
nnoremap <silent> $(sendKey foxdot) :exe 'sil w !python3 -S $(sendblock foxdot)' line('.')<CR>
nnoremap <silent> $(sendKey sclang) :exe 'sil w !python3 -S $(sendblock sclang)' line('.')<CR>
source $(vimrc userPath)
