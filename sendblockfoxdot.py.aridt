from getblock import readblock
import os

def main():
    # TODO LATER: Support more than one session.
    argv = 'screen', '-S', $"$(sessionName), '-p', $"$(foxdot windowName), '-X', 'stuff', readblock()
    os.execvp(argv[0], argv)

if '__main__' == __name__:
    main()
