from getblock import getblock
import os

def main():
    # TODO LATER: Support more than one session.
    argv = 'screen', '-S', $"$(sessionName), '-p', $"$(foxdot windowName), '-X', 'stuff', getblock()
    os.execvp(argv[0], argv)

if '__main__' == __name__:
    main()
