vbell off
split -v
$join$map(
    $(synths)
    $.[screen $join($map($,(synthCommand) w $"$(w)) $.( ))
title $(windowName)
$try($.[stuff $"$(stdin)
] $.[])])focus
resize $(foxdot size)
screen env $"(PYTHONPATH=$(foxdot home)) $"$(Concern interpreter) -m FoxDot --no-startup --pipe
title $(foxdot windowName)
focus
screen vim -u $(vimrcPath) $join($map($(vimArgs) a $"$(a)) $.( ))
title Vim
