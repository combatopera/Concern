vbell off
split -v
$join$map(
    $(synths)
    $.[screen $join($map($,(synthCommand) w $"$(w)) $.( ))
title $(windowName)
$try($.[stuff $"$(stdin)
] $.[])])focus
resize $(foxdot size)
screen $join($map($,(foxdot command) w $"$(w)) $.( ))
title $(foxdot windowName)
focus
screen vim -u $(vimrcPath) $join($map($(vimArgs) a $"$(a)) $.( ))
title Vim
