vbell off
split
$join$map(
    $(synths)
    $.[screen $join($map($,(synthCommand) w $"$(w)) $.( ))
title $(windowName)
$try($.[stuff $"$(stdin)
] $.[])])focus
resize $(foxdot height)
screen env $"(PYTHONPATH=$(foxdot home)) python3 -m FoxDot --no-startup --pipe
title $(foxdot windowName)
focus
screen vim -u $(vimrcPath) $join($map($,(vimArgs) a $"$(a)) $.( ))
title vim
